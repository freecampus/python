---
title: pytest Discovery, Assertions, and Failure Reports
lesson_id: testing-python-programs.pytest-discovery-failures
lesson_order: 20
description: Design fast, meaningful tests at appropriate boundaries and use failures to improve behavior without coupling tests to implementation trivia.
execute:
  echo: true
categories:
- python-foundations
- testing-python-programs
order: 20
colab_notebook: notebooks/courses/python-foundations/units/testing-python-programs/pytest-discovery-failures.ipynb
course_id: python-foundations
unit_id: testing-python-programs
unit_number: 13
---
::: {.lesson-meta}
- **Level:** Python Foundations
- **Estimated time:** 60–90 minutes
- **You will learn:** Derive focused tests from contracts, boundaries, and past defects.
- **Practice in:** Colab, Jupyter, or a local Python project
:::

{{< include ../../../../_includes/colab-link.qmd >}}

## 1. The problem this lesson solves

Strong tests fail for one understandable reason, choose meaningful input classes, and check public behavior. A passing suite is evidence only for the cases and properties it actually exercises.

::: {.callout-note title="Prediction"}
Trace the example by hand. Record the important value, output, or failure before running it.
:::

## 2. Read and trace a complete case

```python
def test_shipping_boundary():
    assert shipping_cost(2) == 5
    assert shipping_cost(2.01) == 8
```

### What Python does

1. The values sit on opposite sides of one threshold.
2. The test checks behavior rather than branch layout.
3. Pytest will display actual and expected values.

Run the example from a clean state. Compare the evidence with your prediction and identify the first point where they differ.

## 3. Debug the boundary

This version is valid Python but does not satisfy the lesson's intended contract:

```python
def test_total():
    prices = [10, 20]
    assert total(prices) == sum(prices)
```

A focused repair is:

```python
def test_total_known_example():
    assert total([10, 20]) == 30
```

Explain the repair in terms of the contract and observable evidence. Do not accept a change only because the error disappeared.

## 4. Practice from trace to transfer

1. Create equivalence classes for age validation.
2. Turn an off-by-one repair into regression test.
3. Read a pytest failure and identify all evidence.

<details class="solution">
<summary>Show a solution strategy after attempting the tasks</summary>

Start with the smallest acceptance example. Keep inputs explicit, implement one behavior, and rerun the matching check. Add a boundary only after the ordinary case is explained. Compare alternative code by correctness and clarity rather than line count.

</details>

## 5. Check your understanding

<script type="application/json" class="fcpython-ojs-quiz-config">
{
  "id": "testing-python-programs-test-design-and-failures",
  "title": "Test Design and Failure Evidence checkpoint",
  "instructions": "Trace the situation before choosing.",
  "questions": [
    {
      "id": "behavior",
      "prompt": "What should tests protect?",
      "options": [
        "Local names",
        "Editor settings",
        "Line count",
        "Observable behavior"
      ],
      "answer_index": 3,
      "explanation": "Behavior survives refactoring."
    },
    {
      "id": "boundary",
      "prompt": "What tests threshold 10?",
      "options": [
        "Values immediately around 10",
        "1 and 2",
        "10 twice",
        "100 only"
      ],
      "answer_index": 0,
      "explanation": "Adjacent cases expose inclusivity."
    },
    {
      "id": "regression",
      "prompt": "What is regression test?",
      "options": [
        "Removed after fix",
        "Reproduces a past defect",
        "A style rule",
        "A random run"
      ],
      "answer_index": 1,
      "explanation": "It prevents the defect returning."
    }
  ]
}
</script>

{{< include ../../../../_includes/ojs-quiz.qmd >}}

## 6. Key points

- Make the behavior and boundary explicit before implementation.
- Trace values and preserve useful failure evidence.
- Prefer an interface that can be checked independently.
- Reuse this skill in the unit challenge rather than treating it as isolated syntax.

## References

- [The Python Tutorial](https://docs.python.org/3/tutorial/)
- [The Python Standard Library](https://docs.python.org/3/library/)
