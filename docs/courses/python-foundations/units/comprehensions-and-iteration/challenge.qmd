---
title: "Unit Challenge: Process a Sensor Stream"
description: "Choose clear eager transformations and lazy iteration for a stream of readings."
challenge_id: comprehensions-and-iteration.challenge
assessment_type: unit-challenge
challenge_format: guided-programming
assessment_version: 1
required_for_completion: true
completion_label: Required unit challenge
estimated_time: "60–120 minutes"
categories: [python-foundations, comprehensions-and-iteration, unit-challenge]
order: 90
colab_notebook: notebooks/courses/python-foundations/units/comprehensions-and-iteration/challenge.ipynb
---

::: {.lesson-meta .challenge-meta}
- **Level:** Python Foundations · Unit 18 challenge
- **Estimated time:** 60–120 minutes
- **Outcome assessed:** Choose clear eager transformations and lazy iteration for a stream of readings.
- **Completion rule:** Complete the required checks, then record completion below.
:::

{{< include ../../../../_includes/colab-link.qmd >}}


<!-- fcpython-unit-challenge: practical -->
## 1. Challenge outcome

Implement a readable summary comprehension and a generator that yields alert readings on demand.

**Scenario:** Sensor records contain an ID and temperature. A dashboard needs an eager mapping of latest values, while an alert consumer needs matching records lazily.

::: {.callout-note title="Work independently, but not without support"}
This challenge should take about one or two focused hours. Use the starter
contract, run one group of assertions at a time, and open hints only after you
can state what evidence is blocking you.
:::

## 2. Understand the acceptance example

Before writing code, predict the important values and the exact final result.
For the supplied input, the expected evidence is:

The mapping preserves IDs and values; the alert generator yields readings at or above a threshold in input order.

Write your prediction in a notebook text cell or project note. Do not change
the sample input until the matching assertions pass.

### Required constraints

1. Use one readable comprehension for `temperature_by_id`.
2. Use `yield` in `alert_readings`; do not build and return a complete alert list.
3. Preserve input order and do not mutate records.
4. Handle empty input without a special fake record.

## 3. Start from the contract

Copy this scaffold into the generated notebook or the indicated local project
files. Preserve the supplied public names and docstrings so the checks can guide
you.

```python
def temperature_by_id(readings):
    """Return an ID-to-temperature dictionary for all readings."""
    raise NotImplementedError


def alert_readings(readings, threshold):
    """Yield readings whose temperature is at least threshold."""
    raise NotImplementedError
    yield
```

## 4. Build in small stages

1. Write the eager dictionary comprehension and check empty input.
2. Implement the generator with an ordinary loop and conditional `yield`.
3. Create the generator without consuming it, then call `next` once and inspect the remaining values.
4. Change the threshold, predict the IDs, and rerun.

After each stage, rerun only the smallest relevant check. When it passes, run
all earlier checks before moving forward.

## 5. Run progressive assertions

These checks describe required observable behavior. A failed assertion is a
location signal: read its message, inspect the actual value, and change one
cause rather than weakening the assertion.

```python
readings = [
    {"id": "a", "temperature": 19.5},
    {"id": "b", "temperature": 24.0},
    {"id": "c", "temperature": 22.0},
]
assert temperature_by_id(readings) == {"a": 19.5, "b": 24.0, "c": 22.0}
assert temperature_by_id([]) == {}

alerts = alert_readings(readings, 22.0)
assert iter(alerts) is alerts, "alert_readings must return an iterator"
assert next(alerts)["id"] == "b"
assert [reading["id"] for reading in alerts] == ["c"]
assert list(alert_readings([], 10.0)) == []
```

::: {.callout-warning title="Do not edit the expected values to create a pass"}
Change the implementation until the stated behavior is true. If you believe an
assertion is wrong, explain the contract conflict in writing before changing
either side.
:::

## 6. Use the hint ladder only when needed

<details>
<summary>Hint 1</summary>

The dictionary comprehension maps `reading["id"]` to `reading["temperature"]`.

</details>

<details>
<summary>Hint 2</summary>

A generator can loop over the original sequence and `yield reading` only when the threshold comparison is true.

</details>

<details>
<summary>Hint 3</summary>

Do not call `list()` inside the generator; the assertions deliberately consume it in two steps.

</details>

## 7. Keep debugging evidence

Keep one exhausted-generator or threshold failure. Trace when each value is requested and yielded, then record the corrected evidence.

Use this short record:

| Failure | Evidence | Hypothesis | Controlled change | Verified rerun |
|---|---|---|---|---|
| What did not work? | What value, output, or error did you observe? | What single cause fits that evidence? | What one thing did you change? | Which check now passes? |

## 8. Check your understanding

::: {.quiz-box}
Answer these questions before recording the challenge. The quiz runs directly
in your browser.
:::

<script type="application/json" class="fcpython-ojs-quiz-config">
{
  "id": "foundations-comprehensions-and-iteration-challenge",
  "title": "Sensor Stream readiness check",
  "instructions": "Choose an answer, then check your work.",
  "questions": [
    {
      "id": "generator-lazy",
      "prompt": "What makes a generator suitable for a long sensor stream?",
      "options": [
        "It can produce matching readings on demand.",
        "It sorts every reading automatically.",
        "It can never be exhausted.",
        "It converts every reading to text."
      ],
      "answer_index": 0,
      "explanation": "Lazy iteration avoids building every possible result before the caller needs it."
    },
    {
      "id": "readable-comprehension",
      "prompt": "When should a comprehension be replaced by an ordinary loop?",
      "options": [
        "When the expression becomes difficult to explain or needs several stateful steps.",
        "Whenever it creates a list.",
        "Whenever it has one condition.",
        "Only when the input is empty."
      ],
      "answer_index": 0,
      "explanation": "Conciseness helps only while the transformation remains readable."
    }
  ]
}
</script>

{{< include ../../../../_includes/ojs-quiz.qmd >}}

## 9. Decide whether the challenge is complete

Record completion only when every statement is true:

- [ ] All supplied assertions pass without editing their expected values.
- [ ] The work runs from a clean notebook state or documented project root.
- [ ] I can explain the important state changes and boundaries in my own words.
- [ ] I kept one complete debugging record.
- [ ] I predicted and verified one controlled modification.

This button stores a self-reported marker only in this browser. It does not
submit the artifact, grade it, verify identity, or issue a certificate.

```{=html}
<div class="fc-assessment-action" data-fc-challenge-action>
  <button class="fc-complete-button" type="button" data-fc-challenge-complete aria-pressed="false">
    <span class="fc-complete-icon" aria-hidden="true">✓</span>
    <span data-fc-challenge-complete-label>Record challenge complete</span>
  </button>
  <p data-fc-challenge-status aria-live="polite">Not yet recorded.</p>
</div>
```

## Key points

- The starter contract narrows the design without supplying the implementation.
- Progressive assertions turn a large task into observable behaviors.
- Hints support the next decision; they do not replace prediction and evidence.
- A clean rerun and explanation matter as much as the final output.
