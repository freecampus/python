---
title: Mutation Across Function Boundaries
lesson_id: mutability-identity-copying.mutation-function-boundaries
lesson_order: 40
description: Predict shared state and choose mutation or copying deliberately, including when objects cross a function boundary.
execute:
  echo: true
categories:
- python-foundations
- mutability-identity-copying
order: 40
colab_notebook: notebooks/courses/python-foundations/units/mutability-identity-copying/mutation-function-boundaries.ipynb
course_id: python-foundations
unit_id: mutability-identity-copying
unit_number: 6
---
::: {.lesson-meta}
- **Level:** Python Foundations
- **Estimated time:** 60–90 minutes
- **You will learn:** Make function side effects explicit and avoid shared mutable defaults.
- **Practice in:** Colab, Jupyter, or a local Python project
:::

{{< include ../../../../_includes/colab-link.qmd >}}

## 1. The problem this lesson solves

Parameters receive references to objects. A function may intentionally mutate caller-owned state or return a transformed copy. The name, docstring, and tests should make that choice visible.

::: {.callout-note title="Prediction"}
Trace the example by hand. Record the important value, output, or failure before running it.
:::

## 2. Read and trace a complete case

```python
def add_tag_in_place(tags, tag):
    """Append tag to tags and return None."""
    tags.append(tag)

labels = ["python"]
print(add_tag_in_place(labels, "course"))
print(labels)
```

### What Python does

1. The caller and parameter refer to one list.
2. Append changes the shared list.
3. Returning `None` reinforces the side-effect contract.

Run the example from a clean state. Compare the evidence with your prediction and identify the first point where they differ.

## 3. Debug the boundary

This version is valid Python but does not satisfy the lesson's intended contract:

```python
def remember(value, history=[]):
    history.append(value)
    return history
```

A focused repair is:

```python
def remember(value, history=None):
    if history is None:
        history = []
    history.append(value)
    return history
```

Explain the repair in terms of the contract and observable evidence. Do not accept a change only because the error disappeared.

## 4. Practice from trace to transfer

1. Test an in-place sorter and a non-mutating sorter.
2. Repair a mutable default dictionary.
3. Rename a function so mutation is obvious.

<details class="solution">
<summary>Show a solution strategy after attempting the tasks</summary>

Start with the smallest acceptance example. Keep inputs explicit, implement one behavior, and rerun the matching check. Add a boundary only after the ordinary case is explained. Compare alternative code by correctness and clarity rather than line count.

</details>

## 5. Check your understanding

<script type="application/json" class="fcpython-ojs-quiz-config">
{
  "id": "mutability-identity-copying-mutation-across-boundaries",
  "title": "Mutation Across Function Boundaries checkpoint",
  "instructions": "Trace the situation before choosing.",
  "questions": [
    {
      "id": "effect",
      "prompt": "What is a side effect?",
      "options": [
        "A returned integer",
        "A type hint",
        "An externally observable change",
        "A local expression"
      ],
      "answer_index": 2,
      "explanation": "Mutation and I/O are effects."
    },
    {
      "id": "append",
      "prompt": "What does list.append return?",
      "options": [
        "The list",
        "The item",
        "The length",
        "None"
      ],
      "answer_index": 3,
      "explanation": "It mutates and returns None."
    },
    {
      "id": "default",
      "prompt": "Why is `items=[]` risky as a default?",
      "options": [
        "The same list is reused across calls",
        "Lists cannot be parameters",
        "It makes tuples",
        "It catches errors"
      ],
      "answer_index": 0,
      "explanation": "Defaults are evaluated at definition time."
    }
  ]
}
</script>

{{< include ../../../../_includes/ojs-quiz.qmd >}}

## 6. Key points

- Make the behavior and boundary explicit before implementation.
- Trace values and preserve useful failure evidence.
- Prefer an interface that can be checked independently.
- Reuse this skill in the unit challenge rather than treating it as isolated syntax.

## References

- [The Python Tutorial](https://docs.python.org/3/tutorial/)
- [The Python Standard Library](https://docs.python.org/3/library/)
