---
title: Equality, Identity, and Object References
lesson_id: mutability-identity-copying.equality-identity-references
lesson_order: 10
description: Predict shared state and choose mutation or copying deliberately, including when objects cross a function boundary.
execute:
  echo: true
categories:
- python-foundations
- mutability-identity-copying
order: 10
colab_notebook: notebooks/courses/python-foundations/units/mutability-identity-copying/equality-identity-references.ipynb
course_id: python-foundations
unit_id: mutability-identity-copying
unit_number: 6
---
::: {.lesson-meta}
- **Level:** Python Foundations
- **Estimated time:** 60–90 minutes
- **You will learn:** Choose assignment, shallow copy, or deep copy after tracing object references.
- **Practice in:** Colab, Jupyter, or a local Python project
:::

{{< include ../../../../_includes/colab-link.qmd >}}

## 1. The problem this lesson solves

Assignment does not copy a mutable object. A shallow copy creates a new outer container but can share nested objects. Equality compares values; identity asks whether references point to one object.

::: {.callout-note title="Prediction"}
Trace the example by hand. Record the important value, output, or failure before running it.
:::

## 2. Read and trace a complete case

```python
original = [["Ada"], ["Grace"]]
shallow = original.copy()
shallow[0].append("Lin")
print(original)
print(original is shallow)
print(original[0] is shallow[0])
```

### What Python does

1. Outer lists are distinct.
2. The first nested list is shared.
3. Mutating the nested list appears through both outer lists.

Run the example from a clean state. Compare the evidence with your prediction and identify the first point where they differ.

## 3. Debug the boundary

This version is valid Python but does not satisfy the lesson's intended contract:

```python
template = []
records = [{"tags": template}, {"tags": template}]
```

A focused repair is:

```python
records = [{"tags": []}, {"tags": []}]
```

Explain the repair in terms of the contract and observable evidence. Do not accept a change only because the error disappeared.

## 4. Practice from trace to transfer

1. Draw references for two names assigned to one dictionary.
2. Compare assignment, shallow copy, and `deepcopy`.
3. Test that records have independent tag lists.

<details class="solution">
<summary>Show a solution strategy after attempting the tasks</summary>

Start with the smallest acceptance example. Keep inputs explicit, implement one behavior, and rerun the matching check. Add a boundary only after the ordinary case is explained. Compare alternative code by correctness and clarity rather than line count.

</details>

## 5. Check your understanding

<script type="application/json" class="fcpython-ojs-quiz-config">
{
  "id": "mutability-identity-copying-identity-aliasing-copying",
  "title": "Identity, Aliasing, and Copying checkpoint",
  "instructions": "Trace the situation before choosing.",
  "questions": [
    {
      "id": "assign",
      "prompt": "What does `alias = items` do?",
      "options": [
        "Deep copies items",
        "Creates another reference",
        "Freezes items",
        "Creates a tuple"
      ],
      "answer_index": 1,
      "explanation": "Assignment binds a second name."
    },
    {
      "id": "equal",
      "prompt": "What does `==` ask?",
      "options": [
        "Whether identities match",
        "Whether names match",
        "Whether values compare equal",
        "Whether values are global"
      ],
      "answer_index": 2,
      "explanation": "Equality normally compares values."
    },
    {
      "id": "nested",
      "prompt": "What can remain shared after shallow copy?",
      "options": [
        "Only the outer identity",
        "All integers globally",
        "The filename",
        "Nested mutable objects"
      ],
      "answer_index": 3,
      "explanation": "Only the outer container is copied."
    }
  ]
}
</script>

{{< include ../../../../_includes/ojs-quiz.qmd >}}

## 6. Key points

- Make the behavior and boundary explicit before implementation.
- Trace values and preserve useful failure evidence.
- Prefer an interface that can be checked independently.
- Reuse this skill in the unit challenge rather than treating it as isolated syntax.

## References

- [The Python Tutorial](https://docs.python.org/3/tutorial/)
- [The Python Standard Library](https://docs.python.org/3/library/)
