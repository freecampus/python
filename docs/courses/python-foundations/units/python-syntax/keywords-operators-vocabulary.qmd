---
title: Keywords, Operators, and Python's Vocabulary
lesson_id: python-syntax.keywords-operators-vocabulary
lesson_order: 20
description: Read a small Python program as tokens, expressions, statements, and blocks; identify what is syntactically valid before reasoning about behavior.
course_id: python-foundations
unit_id: python-syntax
unit_number: 1
execute:
  echo: true
categories:
- python-foundations
- python-syntax
order: 20
colab_notebook: notebooks/courses/python-foundations/units/python-syntax/keywords-operators-vocabulary.ipynb
---
::: {.lesson-meta}
- **Level:** Beginner
- **Estimated time:** 45–70 minutes
- **You will learn:** Read a small Python program as tokens, expressions, statements, and blocks; identify what is syntactically valid before reasoning about behavior.
- **Practice in:** Google Colab, JupyterLab, or a local editor
:::

{{< include ../../../../_includes/colab-link.qmd >}}

## The problem this lesson solves

A beginner can see punctuation as decoration and words such as `for` as ordinary names. Python's vocabulary has roles, and recognizing those roles makes unfamiliar code readable.

Write a prediction before running the example. A useful prediction names the
value or failure you expect and the line responsible for it.

## 1. Read and predict a complete case

```python
import keyword

words = ["total", "for", "return", "student_name"]
for word in words:
    print(word, keyword.iskeyword(word))
```

### Step-by-step explanation

1. `import keyword` makes the standard-library keyword helpers available.
2. The list contains two possible identifiers and two reserved keywords.
3. The loop assigns one string at a time to `word`.
4. `keyword.iskeyword` returns `True` for `for` and `return`; those words cannot be variable names.

::: {.callout-note title="Make one controlled change"}
Add `match`, `price`, and `class` to the list. Predict each Boolean before rerunning.
:::

## 2. Investigate the important boundary

Do not memorize punctuation without context. In `scores[0]`, brackets perform indexing; in `[10, 20]`, they create a list. Read the complete expression and ask what role each token has there.

::: {.callout-warning title="Common mistake"}
Changing several inputs or rules before rerunning destroys the connection
between cause and evidence. Restore the last understood state, change one cause,
and run the smallest relevant check.
:::

## 3. Practice from prediction to explanation

Run this smaller case from a clean state:

```python
price = 12
quantity = 3
subtotal = price * quantity
is_large = subtotal >= 30
print(subtotal, is_large)
```

Then complete the learning loop:

1. Record the output or exception before changing anything.
2. Explain which line establishes each important value.
3. Change one input or boundary named in this lesson.
4. Rerun and explain why the new evidence differs.

<details>
<summary>Check your practice process</summary>

Your record should contain the original prediction, actual result, one
controlled change, new result, and a sentence connecting the changed line to the
new behavior. If it contains only successful output, reproduce and explain one
boundary or failure case as well.

</details>

## 4. Check your understanding

<script type="application/json" class="fcpython-ojs-quiz-config">
{
  "id": "python-syntax-keywords-operators-vocabulary",
  "title": "Keywords, Operators, and Python's Vocabulary checkpoint",
  "instructions": "Choose an answer, then explain the evidence in your own words.",
  "questions": [
    {
      "id": "concept",
      "prompt": "Why can `total` be a name while `return` cannot?",
      "options": [
        "Names must have exactly five letters.",
        "`return` is a reserved keyword with a grammatical role in Python.",
        "Only words beginning with r are forbidden.",
        "`total` is automatically a built-in function."
      ],
      "answer_index": 1,
      "explanation": "Keywords belong to Python's grammar and are unavailable as identifiers."
    },
    {
      "id": "evidence",
      "prompt": "Which record gives the strongest evidence that a controlled modification is understood?",
      "options": [
        "Only a screenshot of the final output.",
        "Several simultaneous edits with no earlier result.",
        "A copied solution whose behavior was not rerun.",
        "A written prediction, the observed result, and an explanation tied to the changed line."
      ],
      "answer_index": 3,
      "explanation": "A controlled experiment connects one predicted cause to an observed effect and an explanation."
    }
  ]
}
</script>

{{< include ../../../../_includes/ojs-quiz.qmd >}}

## Key points

::: {.callout-tip title="Key points"}
- Tokens gain meaning from their role in a complete expression or statement.
- Reserved keywords cannot be used as identifiers.
- Use `keyword.iskeyword` or the language reference instead of relying on memory.
:::

## References

- https://docs.python.org/3/reference/lexical_analysis.html
- https://docs.python.org/3/library/keyword.html
