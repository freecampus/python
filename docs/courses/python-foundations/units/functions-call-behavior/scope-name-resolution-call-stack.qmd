---
title: Scope, Name Resolution, and the Call Stack
lesson_id: functions-call-behavior.scope-name-resolution-call-stack
lesson_order: 40
description: Decompose behavior into functions with clear inputs, outputs, state effects, scopes, and reusable callable patterns.
execute:
  echo: true
categories:
- python-foundations
- functions-call-behavior
order: 40
colab_notebook: notebooks/courses/python-foundations/units/functions-call-behavior/scope-name-resolution-call-stack.ipynb
course_id: python-foundations
unit_id: functions-call-behavior
unit_number: 5
---
::: {.lesson-meta}
- **Level:** Python Foundations
- **Estimated time:** 60–90 minutes
- **You will learn:** Trace name lookup and calls without relying on hidden global mutation.
- **Practice in:** Colab, Jupyter, or a local Python project
:::

{{< include ../../../../_includes/colab-link.qmd >}}

## 1. The problem this lesson solves

Each function call has a frame containing arguments and local names. A call-stack trace records active functions and return points. Python searches local, enclosing, global, then built-in scopes for an unqualified name.

::: {.callout-note title="Prediction"}
Trace the example by hand. Record the important value, output, or failure before running it.
:::

## 2. Read and trace a complete case

```python
tax_rate = 0.2
def total_with_tax(subtotal):
    tax = subtotal * tax_rate
    return subtotal + tax
def invoice_total(prices):
    subtotal = sum(prices)
    return total_with_tax(subtotal)
print(invoice_total([10, 5]))
```

### What Python does

1. The two functions have separate local `subtotal` names.
2. `tax_rate` is read from module scope.
3. Return values move back through the call stack.

Run the example from a clean state. Compare the evidence with your prediction and identify the first point where they differ.

## 3. Debug the boundary

This version is valid Python but does not satisfy the lesson's intended contract:

```python
count = 0
def increment():
    count = count + 1
    return count
```

A focused repair is:

```python
def increment(count):
    return count + 1
```

Explain the repair in terms of the contract and observable evidence. Do not accept a change only because the error disappeared.

## 4. Practice from trace to transfer

1. Trace frames for a function calling two helpers.
2. Create a closure that remembers a label.
3. Refactor a global counter into parameter and return value.

<details class="solution">
<summary>Show a solution strategy after attempting the tasks</summary>

Start with the smallest acceptance example. Keep inputs explicit, implement one behavior, and rerun the matching check. Add a boundary only after the ordinary case is explained. Compare alternative code by correctness and clarity rather than line count.

</details>

## 5. Check your understanding

<script type="application/json" class="fcpython-ojs-quiz-config">
{
  "id": "functions-call-behavior-scope-and-call-stacks",
  "title": "Scope and Call Stacks checkpoint",
  "instructions": "Trace the situation before choosing.",
  "questions": [
    {
      "id": "frame",
      "prompt": "When is a local frame created?",
      "options": [
        "At installation",
        "Only recursively",
        "At program exit",
        "Per function call"
      ],
      "answer_index": 3,
      "explanation": "Every call has its own frame."
    },
    {
      "id": "lookup",
      "prompt": "Where is an unqualified name sought first?",
      "options": [
        "Local",
        "Built-ins",
        "Global",
        "Filesystem"
      ],
      "answer_index": 0,
      "explanation": "Local is first in LEGB."
    },
    {
      "id": "explicit",
      "prompt": "Why pass changing state?",
      "options": [
        "To hide it",
        "To expose dependencies and results",
        "To make it global",
        "To disable scope"
      ],
      "answer_index": 1,
      "explanation": "Explicit data flow is testable."
    }
  ]
}
</script>

{{< include ../../../../_includes/ojs-quiz.qmd >}}

## 6. Key points

- Make the behavior and boundary explicit before implementation.
- Trace values and preserve useful failure evidence.
- Prefer an interface that can be checked independently.
- Reuse this skill in the unit challenge rather than treating it as isolated syntax.

## References

- [The Python Tutorial](https://docs.python.org/3/tutorial/)
- [The Python Standard Library](https://docs.python.org/3/library/)
