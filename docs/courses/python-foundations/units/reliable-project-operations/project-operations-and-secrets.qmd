---
title: Operational Boundaries and Secrets
lesson_id: reliable-project-operations.project-operations-and-secrets
lesson_order: 30
description: Separate configuration, credentials, logs, and deterministic logic.
execute:
  echo: true
categories:
- python-foundations
- reliable-project-operations
order: 30
colab_notebook: notebooks/courses/python-foundations/units/reliable-project-operations/project-operations-and-secrets.ipynb
---
::: {.lesson-meta}
- **Level:** Python Foundations
- **Estimated time:** 60–90 minutes
- **You will learn:** Separate configuration, credentials, logs, and deterministic logic.
- **Practice in:** Colab, Jupyter, or a local Python project
:::

{{< include ../../../../_includes/colab-link.qmd >}}

## 1. The problem this lesson solves

Configuration changes between environments; secrets authorize access and need stronger handling. Validate configuration once at startup, define precedence, and never hard-code or log credentials.

::: {.callout-note title="Prediction"}
Trace the example by hand. Record the important value, output, or failure before running it.
:::

## 2. Read and trace a complete case

```python
import os
from pathlib import Path
def data_path(environ=None):
    source = os.environ if environ is None else environ
    return Path(source.get("APP_DATA_PATH", "data/records.json"))
print(data_path({"APP_DATA_PATH": "tmp/demo.json"}))
```

### What Python does

1. Environment access is injectable for tests.
2. A documented default supports local use.
3. Core calculations should receive values rather than reread environment repeatedly.

Run the example from a clean state. Compare the evidence with your prediction and identify the first point where they differ.

## 3. Debug the boundary

This version is valid Python but does not satisfy the lesson's intended contract:

```python
logger.debug("calling service token=%s", token)
```

A focused repair is:

```python
logger.debug("calling service endpoint=%s", endpoint)
```

Explain the repair in terms of the contract and observable evidence. Do not accept a change only because the error disappeared.

## 4. Practice from trace to transfer

1. Implement CLI-over-env-over-default precedence.
2. Create safe `.env.example` and ignore `.env`.
3. Classify fields as operational or sensitive.

<details class="solution">
<summary>Show a solution strategy after attempting the tasks</summary>

Start with the smallest acceptance example. Keep inputs explicit, implement one behavior, and rerun the matching check. Add a boundary only after the ordinary case is explained. Compare alternative code by correctness and clarity rather than line count.

</details>

## 5. Check your understanding

<script type="application/json" class="fcpython-ojs-quiz-config">
{
  "id": "reliable-project-operations-project-operations-and-secrets",
  "title": "Operational Boundaries and Secrets checkpoint",
  "instructions": "Trace the situation before choosing.",
  "questions": [
    {
      "id": "order",
      "prompt": "Why define precedence?",
      "options": [
        "Hide defaults",
        "Predict selected values",
        "Avoid validation",
        "Commit secrets"
      ],
      "answer_index": 1,
      "explanation": "One order prevents surprises."
    },
    {
      "id": "log",
      "prompt": "What belongs in logs?",
      "options": [
        "API token",
        "Password",
        "Non-sensitive event context",
        "Private key"
      ],
      "answer_index": 2,
      "explanation": "Logs must exclude credentials."
    },
    {
      "id": "env",
      "prompt": "What for real .env?",
      "options": [
        "Commit it",
        "Print in CI",
        "Copy to docs",
        "Ignore it and provide placeholder example"
      ],
      "answer_index": 3,
      "explanation": "Document names without values."
    }
  ]
}
</script>

{{< include ../../../../_includes/ojs-quiz.qmd >}}

## 6. Key points

- Make the behavior and boundary explicit before implementation.
- Trace values and preserve useful failure evidence.
- Prefer an interface that can be checked independently.
- Reuse this skill in the unit challenge rather than treating it as isolated syntax.

## References

- [The Python Tutorial](https://docs.python.org/3/tutorial/)
- [The Python Standard Library](https://docs.python.org/3/library/)
