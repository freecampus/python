---
title: Fixtures, Parametrization, and Boundaries
lesson_id: testing-with-pytest.fixtures-parametrization-and-boundaries
lesson_order: 30
description: Express related cases and isolated setup with pytest.
execute:
  echo: true
categories:
- python-foundations
- testing-with-pytest
order: 30
colab_notebook: notebooks/courses/python-foundations/units/testing-with-pytest/fixtures-parametrization-and-boundaries.ipynb
---
::: {.lesson-meta}
- **Level:** Python Foundations
- **Estimated time:** 60–90 minutes
- **You will learn:** Express related cases and isolated setup with pytest.
- **Practice in:** Colab, Jupyter, or a local Python project
:::

{{< include ../../../../_includes/colab-link.qmd >}}

## 1. The problem this lesson solves

Parametrization runs one rule over several named cases. Fixtures supply setup through arguments and can manage cleanup. Use them to clarify behavior, not hide values that explain failure.

::: {.callout-note title="Prediction"}
Trace the example by hand. Record the important value, output, or failure before running it.
:::

## 2. Read and trace a complete case

```python
import pytest
@pytest.mark.parametrize(("score", "expected"),
    [(59, "F"), (60, "D"), (89, "B"), (90, "A")])
def test_grade_boundaries(score, expected):
    assert letter_grade(score) == expected
```

### What Python does

1. Each tuple becomes a separate reported case.
2. Values surround boundaries.
3. One test body states the shared rule.

Run the example from a clean state. Compare the evidence with your prediction and identify the first point where they differ.

## 3. Debug the boundary

This version is valid Python but does not satisfy the lesson's intended contract:

```python
shared = []
@pytest.fixture
def records():
    return shared
```

A focused repair is:

```python
@pytest.fixture
def records():
    return []
```

Explain the repair in terms of the contract and observable evidence. Do not accept a change only because the error disappeared.

## 4. Practice from trace to transfer

1. Parametrize valid and invalid dates.
2. Use `tmp_path` for save/load round trip.
3. Inject a clock instead of patching internals.

<details class="solution">
<summary>Show a solution strategy after attempting the tasks</summary>

Start with the smallest acceptance example. Keep inputs explicit, implement one behavior, and rerun the matching check. Add a boundary only after the ordinary case is explained. Compare alternative code by correctness and clarity rather than line count.

</details>

## 5. Check your understanding

<script type="application/json" class="fcpython-ojs-quiz-config">
{
  "id": "testing-with-pytest-fixtures-parametrization-and-boundaries",
  "title": "Fixtures, Parametrization, and Boundaries checkpoint",
  "instructions": "Trace the situation before choosing.",
  "questions": [
    {
      "id": "param",
      "prompt": "What does parametrization provide?",
      "options": [
        "One global fixture",
        "Production code",
        "Several cases for one rule",
        "Git history"
      ],
      "answer_index": 2,
      "explanation": "It reports related examples separately."
    },
    {
      "id": "tmp",
      "prompt": "What is tmp_path?",
      "options": [
        "User home",
        "Mock network",
        "Argument list",
        "Isolated temporary Path"
      ],
      "answer_index": 3,
      "explanation": "It isolates filesystem tests."
    },
    {
      "id": "fresh",
      "prompt": "What should mutable function fixture return?",
      "options": [
        "Fresh state per test",
        "One global list",
        "Test result",
        "A file cache"
      ],
      "answer_index": 0,
      "explanation": "Isolation avoids order dependence."
    }
  ]
}
</script>

{{< include ../../../../_includes/ojs-quiz.qmd >}}

## 6. Key points

- Make the behavior and boundary explicit before implementation.
- Trace values and preserve useful failure evidence.
- Prefer an interface that can be checked independently.
- Reuse this skill in the unit challenge rather than treating it as isolated syntax.

## References

- [The Python Tutorial](https://docs.python.org/3/tutorial/)
- [The Python Standard Library](https://docs.python.org/3/library/)
