---
title: Search, Count, Aggregate, Filter, and Transform Patterns
lesson_id: problem-solving-algorithms.core-algorithm-patterns
lesson_order: 30
description: Convert an ambiguous task into examples, constraints, an algorithm, small functions, and evidence that the result handles important boundaries.
execute:
  echo: true
categories:
- python-foundations
- problem-solving-algorithms
order: 30
colab_notebook: notebooks/courses/python-foundations/units/problem-solving-algorithms/core-algorithm-patterns.ipynb
course_id: python-foundations
unit_id: problem-solving-algorithms
unit_number: 7
---
::: {.lesson-meta}
- **Level:** Python Foundations
- **Estimated time:** 60–90 minutes
- **You will learn:** Recognize common loop patterns and reason about correctness and basic cost.
- **Practice in:** Colab, Jupyter, or a local Python project
:::

{{< include ../../../../_includes/colab-link.qmd >}}

## 1. The problem this lesson solves

Many tasks find one item, count matches, accumulate a result, or transform every item. Naming the pattern reduces blank-page pressure while the contract still determines sentinels, stopping behavior, and output shape.

::: {.callout-note title="Prediction"}
Trace the example by hand. Record the important value, output, or failure before running it.
:::

## 2. Read and trace a complete case

```python
temperatures = [18, 21, 25, 19]
first_hot = None
for temperature in temperatures:
    if temperature >= 24:
        first_hot = temperature
        break
print(first_hot)
```

### What Python does

1. `None` records that no match has been found yet.
2. Items are inspected in order.
3. Stopping is correct because the contract asks for the first match.

Run the example from a clean state. Compare the evidence with your prediction and identify the first point where they differ.

## 3. Debug the boundary

This version is valid Python but does not satisfy the lesson's intended contract:

```python
known = ["a", "b", "c"]
found = [item for item in items if item in known]
```

A focused repair is:

```python
known = {"a", "b", "c"}
found = [item for item in items if item in known]
```

Explain the repair in terms of the contract and observable evidence. Do not accept a change only because the error disappeared.

## 4. Practice from trace to transfer

1. Implement `find_first_negative` with `None` for no match.
2. Count case-normalized words.
3. Compare a nested duplicate search with a set-based trace.

<details class="solution">
<summary>Show a solution strategy after attempting the tasks</summary>

Start with the smallest acceptance example. Keep inputs explicit, implement one behavior, and rerun the matching check. Add a boundary only after the ordinary case is explained. Compare alternative code by correctness and clarity rather than line count.

</details>

## 5. Check your understanding

<script type="application/json" class="fcpython-ojs-quiz-config">
{
  "id": "problem-solving-algorithms-search-count-transform",
  "title": "Search, Count, and Transform Patterns checkpoint",
  "instructions": "Trace the situation before choosing.",
  "questions": [
    {
      "id": "stop",
      "prompt": "Why may first-match search stop?",
      "options": [
        "All loops need break",
        "None stops loops",
        "It changes scope",
        "Later matches cannot replace the first"
      ],
      "answer_index": 3,
      "explanation": "The contract is already satisfied."
    },
    {
      "id": "count",
      "prompt": "What state does counting update?",
      "options": [
        "A counter or mapping",
        "A traceback",
        "The Python version",
        "A filename"
      ],
      "answer_index": 0,
      "explanation": "Counts persist between iterations."
    },
    {
      "id": "linear",
      "prompt": "When input doubles, one full pass does roughly what?",
      "options": [
        "Half the work",
        "Double the work",
        "The same work",
        "Four times the work"
      ],
      "answer_index": 1,
      "explanation": "One operation per item is linear intuition."
    }
  ]
}
</script>

{{< include ../../../../_includes/ojs-quiz.qmd >}}

## 6. Key points

- Make the behavior and boundary explicit before implementation.
- Trace values and preserve useful failure evidence.
- Prefer an interface that can be checked independently.
- Reuse this skill in the unit challenge rather than treating it as isolated syntax.

## References

- [The Python Tutorial](https://docs.python.org/3/tutorial/)
- [The Python Standard Library](https://docs.python.org/3/library/)
