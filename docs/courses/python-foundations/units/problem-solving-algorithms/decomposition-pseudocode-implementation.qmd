---
title: Decomposition, Pseudocode, and Incremental Implementation
lesson_id: problem-solving-algorithms.decomposition-pseudocode-implementation
lesson_order: 20
description: Convert an ambiguous task into examples, constraints, an algorithm, small functions, and evidence that the result handles important boundaries.
execute:
  echo: true
categories:
- python-foundations
- problem-solving-algorithms
order: 20
colab_notebook: notebooks/courses/python-foundations/units/problem-solving-algorithms/decomposition-pseudocode-implementation.ipynb
course_id: python-foundations
unit_id: problem-solving-algorithms
unit_number: 7
---
::: {.lesson-meta}
- **Level:** Python Foundations
- **Estimated time:** 60–90 minutes
- **You will learn:** Split a task into named, checkable responsibilities before choosing syntax.
- **Practice in:** Colab, Jupyter, or a local Python project
:::

{{< include ../../../../_includes/colab-link.qmd >}}

## 1. The problem this lesson solves

Parsing, validation, calculation, and presentation fail for different reasons. Decomposition gives each boundary a name and lets you test one stage before the whole application exists.

::: {.callout-note title="Prediction"}
Trace the example by hand. Record the important value, output, or failure before running it.
:::

## 2. Read and trace a complete case

```python
def build_report(lines):
    records = parse_lines(lines)
    valid = validate_records(records)
    totals = total_by_category(valid)
    return format_report(totals)
```

### What Python does

1. Each function has one responsibility.
2. Data changes shape at explicit boundaries.
3. The earliest wrong boundary narrows debugging.

Run the example from a clean state. Compare the evidence with your prediction and identify the first point where they differ.

## 3. Debug the boundary

This version is valid Python but does not satisfy the lesson's intended contract:

```python
def do_everything(path):
    # read, validate, calculate, print, and catch every error
    pass
```

A focused repair is:

```python
def summarize(records):
    valid = validate_records(records)
    return total_by_category(valid)
```

Explain the repair in terms of the contract and observable evidence. Do not accept a change only because the error disappeared.

## 4. Practice from trace to transfer

1. Write verb-first pseudocode for finding duplicate names.
2. Separate input, calculation, and output in a receipt program.
3. Draw the data shape after each pipeline stage.

<details class="solution">
<summary>Show a solution strategy after attempting the tasks</summary>

Start with the smallest acceptance example. Keep inputs explicit, implement one behavior, and rerun the matching check. Add a boundary only after the ordinary case is explained. Compare alternative code by correctness and clarity rather than line count.

</details>

## 5. Check your understanding

<script type="application/json" class="fcpython-ojs-quiz-config">
{
  "id": "problem-solving-algorithms-decomposition-and-pseudocode",
  "title": "Decomposition and Pseudocode checkpoint",
  "instructions": "Trace the situation before choosing.",
  "questions": [
    {
      "id": "boundary",
      "prompt": "Which boundary is valuable first?",
      "options": [
        "One function per line",
        "A class per value",
        "Separate I/O from calculation",
        "One global dict"
      ],
      "answer_index": 2,
      "explanation": "I/O and calculation have distinct failures."
    },
    {
      "id": "pseudo",
      "prompt": "What should pseudocode emphasize?",
      "options": [
        "Exact punctuation",
        "Package versions",
        "Line count",
        "Decisions and data movement"
      ],
      "answer_index": 3,
      "explanation": "It supports reasoning before syntax."
    },
    {
      "id": "debug",
      "prompt": "Where should pipeline debugging start?",
      "options": [
        "At the earliest incorrect output",
        "At a random stage",
        "At the last print",
        "By rewriting all stages"
      ],
      "answer_index": 0,
      "explanation": "The first wrong stage narrows the cause."
    }
  ]
}
</script>

{{< include ../../../../_includes/ojs-quiz.qmd >}}

## 6. Key points

- Make the behavior and boundary explicit before implementation.
- Trace values and preserve useful failure evidence.
- Prefer an interface that can be checked independently.
- Reuse this skill in the unit challenge rather than treating it as isolated syntax.

## References

- [The Python Tutorial](https://docs.python.org/3/tutorial/)
- [The Python Standard Library](https://docs.python.org/3/library/)
