---
title: Recursion and Functions as Values
lesson_id: scope-and-call-stacks.recursion-and-functions-as-values
lesson_order: 20
description: Trace recursive progress and pass functions as configurable behavior.
execute:
  echo: true
categories:
- python-foundations
- scope-and-call-stacks
order: 20
colab_notebook: notebooks/courses/python-foundations/units/scope-and-call-stacks/recursion-and-functions-as-values.ipynb
---
::: {.lesson-meta}
- **Level:** Python Foundations
- **Estimated time:** 60–90 minutes
- **You will learn:** Trace recursive progress and pass functions as configurable behavior.
- **Practice in:** Colab, Jupyter, or a local Python project
:::

{{< include ../../../../_includes/colab-link.qmd >}}

## 1. The problem this lesson solves

Recursion calls the same function on a smaller problem and requires a reachable base case. Functions are also values: `sorted(key=...)` receives behavior without calling it in advance.

::: {.callout-note title="Prediction"}
Trace the example by hand. Record the important value, output, or failure before running it.
:::

## 2. Read and trace a complete case

```python
def total_minutes(node):
    if isinstance(node, int):
        return node
    return sum(total_minutes(child) for child in node)
print(total_minutes([25, [10, 15], 20]))
```

### What Python does

1. Integers are base cases.
2. Lists trigger recursive calls on smaller nodes.
3. Results combine as frames return.

Run the example from a clean state. Compare the evidence with your prediction and identify the first point where they differ.

## 3. Debug the boundary

This version is valid Python but does not satisfy the lesson's intended contract:

```python
def countdown(number):
    if number == 0:
        return
    countdown(number)
```

A focused repair is:

```python
def countdown(number):
    if number <= 0:
        return
    countdown(number - 1)
```

Explain the repair in terms of the contract and observable evidence. Do not accept a change only because the error disappeared.

## 4. Practice from trace to transfer

1. Trace `factorial(4)` as frames and returns.
2. Compare iterative and recursive solutions for flat data.
3. Sort records with a named key function.

<details class="solution">
<summary>Show a solution strategy after attempting the tasks</summary>

Start with the smallest acceptance example. Keep inputs explicit, implement one behavior, and rerun the matching check. Add a boundary only after the ordinary case is explained. Compare alternative code by correctness and clarity rather than line count.

</details>

## 5. Check your understanding

<script type="application/json" class="fcpython-ojs-quiz-config">
{
  "id": "scope-and-call-stacks-recursion-and-functions-as-values",
  "title": "Recursion and Functions as Values checkpoint",
  "instructions": "Trace the situation before choosing.",
  "questions": [
    {
      "id": "base",
      "prompt": "What does a base case do?",
      "options": [
        "Stops recursion",
        "Sorts input",
        "Creates globals",
        "Catches all errors"
      ],
      "answer_index": 0,
      "explanation": "It terminates recursive calls."
    },
    {
      "id": "progress",
      "prompt": "What must recursion do?",
      "options": [
        "Keep input unchanged",
        "Move toward the base case",
        "Use lambda",
        "Print every value"
      ],
      "answer_index": 1,
      "explanation": "The problem must become smaller."
    },
    {
      "id": "key",
      "prompt": "What does `key=` receive?",
      "options": [
        "A called result",
        "A module path",
        "A function returning comparison keys",
        "Only a bool"
      ],
      "answer_index": 2,
      "explanation": "Sorted calls the function for items."
    }
  ]
}
</script>

{{< include ../../../../_includes/ojs-quiz.qmd >}}

## 6. Key points

- Make the behavior and boundary explicit before implementation.
- Trace values and preserve useful failure evidence.
- Prefer an interface that can be checked independently.
- Reuse this skill in the unit challenge rather than treating it as isolated syntax.

## References

- [The Python Tutorial](https://docs.python.org/3/tutorial/)
- [The Python Standard Library](https://docs.python.org/3/library/)
