---
title: Built-ins, the Standard Library, Third-Party Packages, and Help
lesson_id: modules-environments-projects.builtins-standard-library-help
lesson_order: 10
description: Find reusable functionality, split code across modules, organize a versioned project, isolate dependencies, and explain how source becomes an installable distribution.
execute:
  echo: true
categories:
- python-foundations
- modules-environments-projects
order: 10
colab_notebook: notebooks/courses/python-foundations/units/modules-environments-projects/builtins-standard-library-help.ipynb
course_id: python-foundations
unit_id: modules-environments-projects
unit_number: 10
---
::: {.lesson-meta}
- **Level:** Python Foundations
- **Estimated time:** 60–90 minutes
- **You will learn:** Find, evaluate, and apply documented standard-library tools.
- **Practice in:** Colab, Jupyter, or a local Python project
:::

{{< include ../../../../_includes/colab-link.qmd >}}

## 1. The problem this lesson solves

Fluency means discovering tools, reading their contracts, trying a small example, and recording important assumptions—not memorizing every module. Standard modules still need application-level tests.

::: {.callout-note title="Prediction"}
Trace the example by hand. Record the important value, output, or failure before running it.
:::

## 2. Read and trace a complete case

```python
from collections import Counter
from statistics import mean
counts = Counter("red blue red green red".split())
print(counts.most_common(2))
print(mean(counts.values()))
```

### What Python does

1. Counter models frequencies.
2. `most_common` returns item/count pairs.
3. `mean` communicates statistical intent.

Run the example from a clean state. Compare the evidence with your prediction and identify the first point where they differ.

## 3. Debug the boundary

This version is valid Python but does not satisfy the lesson's intended contract:

```python
from statistics import mean  # local statistics.py shadows stdlib
print(mean([1, 2, 3]))
```

A focused repair is:

```python
import statistics
print(statistics.__file__)
print(statistics.mean([1, 2, 3]))
```

Explain the repair in terms of the contract and observable evidence. Do not accept a change only because the error disappeared.

## 4. Practice from trace to transfer

1. Use `help(Counter.most_common)` and summarize its result.
2. Replace a manual frequency loop with Counter.
3. Use datetime across a month boundary.

<details class="solution">
<summary>Show a solution strategy after attempting the tasks</summary>

Start with the smallest acceptance example. Keep inputs explicit, implement one behavior, and rerun the matching check. Add a boundary only after the ordinary case is explained. Compare alternative code by correctness and clarity rather than line count.

</details>

## 5. Check your understanding

<script type="application/json" class="fcpython-ojs-quiz-config">
{
  "id": "modules-environments-projects-standard-library-fluency",
  "title": "Standard-Library Fluency checkpoint",
  "instructions": "Trace the situation before choosing.",
  "questions": [
    {
      "id": "fluency",
      "prompt": "What is library fluency?",
      "options": [
        "Find and check documented tools",
        "Memorize all names",
        "Use star imports",
        "Avoid tests"
      ],
      "answer_index": 0,
      "explanation": "Discovery and contract reading matter."
    },
    {
      "id": "counter",
      "prompt": "Which models frequencies?",
      "options": [
        "Path",
        "Counter",
        "Decimal",
        "date"
      ],
      "answer_index": 1,
      "explanation": "Counter is designed for counts."
    },
    {
      "id": "location",
      "prompt": "What reveals many module locations?",
      "options": [
        "__count__",
        "__path_name__",
        "__file__",
        "__test__"
      ],
      "answer_index": 2,
      "explanation": "Inspecting location exposes shadowing."
    }
  ]
}
</script>

{{< include ../../../../_includes/ojs-quiz.qmd >}}

## 6. Key points

- Make the behavior and boundary explicit before implementation.
- Trace values and preserve useful failure evidence.
- Prefer an interface that can be checked independently.
- Reuse this skill in the unit challenge rather than treating it as isolated syntax.

## References

- [The Python Tutorial](https://docs.python.org/3/tutorial/)
- [The Python Standard Library](https://docs.python.org/3/library/)
